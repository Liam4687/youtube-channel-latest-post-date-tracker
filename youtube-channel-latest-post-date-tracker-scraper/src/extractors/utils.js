js// Utility functions for extracting and processing data (if needed)

// Placeholder function for future utility functions
function formatDate(date) {
  return new Date(date).toISOString();
}

module.exports = { formatDate };